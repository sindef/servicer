---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: clustertargets.platform.servicer.io
spec:
  group: platform.servicer.io
  names:
    kind: ClusterTarget
    listKind: ClusterTargetList
    plural: clustertargets
    shortNames:
    - ctarget
    singular: clustertarget
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.reachable
      name: Reachable
      type: boolean
    - jsonPath: .status.kubernetesVersion
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterTarget represents a workload or management cluster the
          platform can place products onto.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterTargetSpec defines the desired state of a ClusterTarget.
            properties:
              capabilities:
                additionalProperties:
                  type: string
                description: Capabilities describes cluster capabilities used for
                  placement decisions.
                type: object
              connectionRef:
                description: ConnectionRef identifies the Kubernetes Secret holding
                  cluster access credentials.
                properties:
                  name:
                    description: Name is the resource name.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the resource namespace.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              defaultPolicyRefs:
                description: DefaultPolicyRefs references policy bundles applied by
                  default to products placed here.
                items:
                  description: PolicyReference points to a policy bundle or policy
                    object.
                  properties:
                    name:
                      description: Name identifies the referenced policy bundle.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              displayName:
                description: DisplayName is the human-readable name for the cluster
                  target.
                minLength: 1
                type: string
              ingressDomain:
                description: IngressDomain identifies the default external DNS suffix
                  for products on this cluster.
                type: string
              region:
                description: Region identifies the logical region or site of the cluster.
                type: string
            required:
            - connectionRef
            - displayName
            type: object
          status:
            description: ClusterTargetStatus defines the observed state of a ClusterTarget.
            properties:
              conditions:
                description: Conditions contains the current status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              kubernetesVersion:
                description: KubernetesVersion records the discovered Kubernetes server
                  version.
                type: string
              lastValidatedAt:
                description: LastValidatedAt records the last successful connectivity
                  or capability validation time.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent processed generation.
                format: int64
                type: integer
              operatorInventory:
                description: OperatorInventory lists the discovered operators available
                  on the target cluster.
                items:
                  type: string
                type: array
              phase:
                description: Phase is the coarse lifecycle summary.
                type: string
              reachable:
                description: Reachable indicates whether the cluster is currently
                  reachable.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
