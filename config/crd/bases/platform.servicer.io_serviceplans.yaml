---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: serviceplans.platform.servicer.io
spec:
  group: platform.servicer.io
  names:
    kind: ServicePlan
    listKind: ServicePlanList
    plural: serviceplans
    shortNames:
    - splan
    singular: serviceplan
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceClassRef.name
      name: Class
      type: string
    - jsonPath: .spec.topology
      name: Topology
      type: string
    - jsonPath: .status.published
      name: Published
      type: boolean
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServicePlan defines a curated plan for a service class.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServicePlanSpec defines the desired state of a ServicePlan.
            properties:
              allowsVersionOverride:
                description: AllowsVersionOverride indicates whether instances may
                  override the plan's version.
                type: boolean
              constraints:
                description: Constraints captures plan-specific validation constraints.
                x-kubernetes-preserve-unknown-fields: true
              defaultParameters:
                description: DefaultParameters provides plan-scoped default parameters.
                x-kubernetes-preserve-unknown-fields: true
              defaultVersion:
                description: DefaultVersion identifies the default engine version
                  for this plan.
                type: string
              displayName:
                description: DisplayName is the human-readable name for the plan.
                minLength: 1
                type: string
              policyRefs:
                description: PolicyRefs references policy bundles applied by default
                  to instances of this plan.
                items:
                  description: PolicyReference points to a policy bundle or policy
                    object.
                  properties:
                    name:
                      description: Name identifies the referenced policy bundle.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              serviceClassRef:
                description: ServiceClassRef references the parent service class for
                  the plan.
                properties:
                  name:
                    description: Name is the stable resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              tier:
                description: Tier identifies the commercial or operational tier such
                  as starter or ha.
                type: string
              topology:
                description: Topology identifies the intended runtime topology such
                  as single-primary or high-availability.
                type: string
            required:
            - displayName
            - serviceClassRef
            type: object
          status:
            description: ServicePlanStatus defines the observed state of a ServicePlan.
            properties:
              conditions:
                description: Conditions contains the current status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent processed generation.
                format: int64
                type: integer
              phase:
                description: Phase is the coarse lifecycle summary.
                type: string
              published:
                description: Published is the observed publication state of the plan.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
